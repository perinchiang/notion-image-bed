name: Compress Images
on:
  push:
    paths:
      - 'images/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  compress:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y jpegoptim pngquant imagemagick

      - name: Compress Images
        run: |
          echo "📊 压缩前：最大的 5 张图片体积："
          ls -lhS images | head -n 6
          
          echo "🚀 开始暴力压缩..."
          
          # 1. 统一尺寸限制 (忽略错误继续执行)
          find images -type f \( -iname "*.png" -o -iname "*.jpg" -o -iname "*.jpeg" \) | xargs -r mogrify -resize '1920>' || true

          # 2. JPG 压缩 (忽略错误继续执行)
          find images -type f \( -iname "*.jpg" -o -iname "*.jpeg" \) | xargs -r jpegoptim -m75 --strip-all || true

          # 3. PNG 暴力压缩 (重点是最后的 || true)
          # 这会防止因为跳过某张图而导致整个脚本崩溃
          find images -type f -iname "*.png" | xargs -r pngquant --quality=50-80 --speed 1 --ext .png --force --skip-if-larger || true

          echo "🎉 压缩后：最大的 5 张图片体积："
          ls -lhS images | head -n 6

      - name: Commit & Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          if [[ -n $(git status -s) ]]; then
            git add images/*
            git commit -m "📉 Final Compress (Safe Mode) [skip ci]"
            git push
            echo "✅ 图片已优化并推送到仓库"
          else
            echo "✨ 没有发现可以进一步压缩的图片"
          fi
